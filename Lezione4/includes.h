//There are many include files. Here are the most useful:
#include <stdio.h>
//Includes IO operations such as:
/* printf(), fprintf(), scanf(), fscanf()
* getc(), putc(), fopen(), fclose() etc...*/

#include <stdlib.h>

/* abs(), srand(), rand(), malloc(), free() */

#include <math.h>
/* sin(), cos(), tan(), pow(), log(), exp(), sqrt(), 
ceil(), floor(), trunc(), round()*/

