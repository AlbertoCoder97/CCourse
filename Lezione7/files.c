/* There are multiple functions to read/write from/to a file. 
* - fread(inArea, sizeof (int), 3, spData) reads a block of bytes from the file and stores it in memory. 
* - fwrite() is the same but for writing from memory to disk. 
*/