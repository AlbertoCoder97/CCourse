#include "iofunctions.h"

/* Useful I/O Functions*/
/* Terminal Only functions:
* getchar() is an input function
* putchar() is an output function */

/* Array Stream functions:
* getc()/fgetc() are input functions
* putc()/fputc() are output functions
* ungetc() is a particular function that if the read character is not needed pushes it back
* and reads it again if you read chars from a stream again. You can use it to get out of loops
* if your tokens contain unexpected chars. */

int main()
{
    printf("WTF\n");
    return 0;
}